<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FirstProject"
             x:Class="FirstProject.MainPage">

    <StackLayout Margin="16,20,16,0">
        <!-- Place new controls here -->
        <Entry Placeholder="Title of experience..." 
               x:Name="titleExperience" 
               Style="{StaticResource mainTitleStyle}"
               TextChanged="TitleExperience_TextChanged"/>
        <Editor Placeholder="Content of experience"
                VerticalOptions="FillAndExpand"
                x:Name="contentExperience" 
                Style="{StaticResource mainContentStyle}"
                TextChanged="ContentExperience_TextChanged"/>
        <Button Text="Save" 
                x:Name="saveBtn"
                Style="{StaticResource mainButtonStyle}"
                IsEnabled="false"
                Clicked="Save_Clicked"/>
        <Button Text="Cancel" 
                Style="{StaticResource cancelButtonStyle}"
                x:Name="cancelBtn"/>
    </StackLayout>
</ContentPage>
